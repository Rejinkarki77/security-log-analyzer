# Optional: Microsoft Teams Incoming Webhook URL.
# When set, a high-severity alert (e.g. credential stuffing success) will
# post a summary card to this Teams channel. Leave blank to skip alerting
# and just run the pipeline + dashboard.
TEAMS_WEBHOOK_URL=
